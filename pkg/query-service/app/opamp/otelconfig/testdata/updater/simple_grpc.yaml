receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "http://localhost:6969"
      http:
exporters:
  jaeger:
    endpoint: "http://localhost:14250"
service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: []
      exporters: [jaeger, otlp]
