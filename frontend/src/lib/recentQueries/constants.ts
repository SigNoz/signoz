export const STORAGE_KEY_PREFIX = 'qb_recent_v1';
export const STORAGE_VERSION = 1;
// Maximum entries kept per (signal, source) bucket. Matches RECENTS_DISPLAY_CAP
// in QuerySearch so the store and the dropdown agree on a single cap.
export const MAX_ENTRIES = 5;
