// Shared column count for the single mappings table. Group header rows and
// per-group state rows (loading / empty / error) span all columns; keeping the
// value in one place stops the header and its full-width rows drifting apart.
export const COLUMN_COUNT = 4;
