import { Card } from 'antd';
import styled from 'styled-components';

export const DropDownContainer = styled(Card)`
	top: 0.5rem;
	position: absolute;
	width: 100%;
	z-index: 1;
	.ant-card-body {
		padding: 0.8rem;
	}
`;
