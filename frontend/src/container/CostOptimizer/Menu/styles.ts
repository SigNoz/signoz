import { Card } from 'antd';
import styled from 'styled-components';

export const CardMenuContainer = styled.div`
	&&& {
		padding: 1rem;
	}
`;

export const MenuOptionCard = styled(Card)`
	&&& {
		margin: 5px;
		width: 21rem;
		cursor: pointer;
	}
`;
